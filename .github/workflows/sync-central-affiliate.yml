name: Synchronizace centrální affiliate databáze
on:
  push:
    branches: [main]
    paths: ['assets/affiliate-site-config.json','.github/workflows/sync-central-affiliate.yml','.github/workflows/reusable-affiliate-sync-public.yml']
  issues:
    types: [opened]
  schedule:
    - cron: '47 */6 * * *'
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: central-affiliate-${{ github.repository }}
  cancel-in-progress: true
jobs:
  sync:
    if: github.event_name != 'issues' || github.event.issue.title == '[affiliate-sync] trigger'
    uses: ./.github/workflows/reusable-affiliate-sync-public.yml
    with:
      runs_on: '["self-hosted","Linux","X64"]'
