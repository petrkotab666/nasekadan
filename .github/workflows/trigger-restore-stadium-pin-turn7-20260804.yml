name: Spustit obnovu připnutí zimního stadionu – kontrola 20:26

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/trigger-restore-stadium-pin-turn7-20260804.yml'

permissions:
  actions: write
  contents: read

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Spustit existující bezpečný obnovovací postup
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run restore-stadium-pin-active-20260804-2000.yml --repo petrkotab666/nasekadan --ref main
