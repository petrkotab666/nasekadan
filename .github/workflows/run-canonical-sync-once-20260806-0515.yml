name: Spustit jednorázový živý kanonický audit 2026-08-06 05:15

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/run-canonical-sync-once-20260806-0515.yml'

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Spustit kanonickou synchronizaci
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run sync-canonical-published-index-20260803-2230.yml --repo petrkotab666/nasekadan --ref main

# trigger 2026-08-06T03:18Z
