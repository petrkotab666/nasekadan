name: Jednorázově spustit synchronizaci kanonického registru 20260806-0417

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/run-canonical-sync-once-20260806-0417.yml'

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Spustit živou synchronizaci registru
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run sync-canonical-published-index-20260803-2230.yml --ref main

# trigger 2026-08-06T02:20Z
