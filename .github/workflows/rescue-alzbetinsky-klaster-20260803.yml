name: Poslední záchranná pojistka článku o klášteře

on:
  schedule:
    - cron: '50 2 3 8 *'
  workflow_dispatch:

permissions:
  contents: write
  issues: write

concurrency:
  group: publish-alzbetinsky-klaster-20260803-rescue
  cancel-in-progress: false

env:
  KEY_OVH_SSH_KEY: ${{ secrets.OVH_SSH_KEY }}
  KEY_OVH_SSH_PRIVATE_KEY: ${{ secrets.OVH_SSH_PRIVATE_KEY }}
  KEY_SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
  KEY_VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
  KEY_VPS_SSH_PRIVATE_KEY: ${{ secrets.VPS_SSH_PRIVATE_KEY }}

jobs:
  rescue:
    runs-on: ubuntu-latest
    timeout-minutes: 32
    steps:
      - name: Povolit pouze 3. srpna 2026
        shell: bash
        run: |
          test "$(date -u +%Y-%m-%d)" = "2026-08-03" || exit 0

      - name: Poslední nezávislá kontrola
        id: before
        shell: bash
        run: |
          set +e
          article="$(curl -fsSL --max-time 25 -H 'Cache-Control: no-cache' "https://nasekadan.cz/clanky/alzbetinsky-klaster-kadan-pacienti-lecebna-1966.html?rescue=${GITHUB_RUN_ID}" 2>/dev/null)"
          home="$(curl -fsSL --max-time 25 -H 'Cache-Control: no-cache' "https://nasekadan.cz/?rescue=${GITHUB_RUN_ID}" 2>/dev/null)"
          image_size="$(curl -fsSL --max-time 25 "https://nasekadan.cz/social/alzbetinsky-klaster-kadan-pacienti-20260803.png?rescue=1" 2>/dev/null | wc -c)"
          if grep -Fq 'Kam odešli poslední pacienti?' <<<"$article" &&
             grep -Fq '"ok": true' <<<"$(curl -fsSL --max-time 25 "https://nasekadan.cz/api/newsletter/poll/results?poll=alzbetinsky-klaster-kadan-2026" 2>/dev/null)" &&
             grep -Fq 'data-latest-article-href="/clanky/alzbetinsky-klaster-kadan-pacienti-lecebna-1966.html"' <<<"$home" &&
             [[ "$image_size" -gt 10000 ]]; then
            echo 'present=true' >> "$GITHUB_OUTPUT"
          else
            echo 'present=false' >> "$GITHUB_OUTPUT"
          fi

      - name: Stáhnout hlavní větev
        if: steps.before.outputs.present != 'true'
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Poslední úplná obnova
        if: steps.before.outputs.present != 'true'
        shell: bash
        run: |
          set -euo pipefail
          python3 -m pip install --disable-pip-version-check --quiet pillow
          python3 scripts/publish_alzbetinsky_klaster_20260803.py
          git config user.name 'Naše Kadaň – poslední záchrana kláštera'
          git config user.email 'info@nasekadan.cz'
          git add clanky/alzbetinsky-klaster-kadan-pacienti-lecebna-1966.html social/alzbetinsky-klaster-kadan-pacienti-20260803.png index.html clanky/index.html rss.xml sitemap.xml news-sitemap.xml
          git commit -m 'Poslední obnova publikace kláštera [skip ci]' || true
          for attempt in 1 2 3 4 5; do
            if git pull --rebase origin main && git push origin HEAD:main; then break; fi
            git rebase --abort 2>/dev/null || true
            [[ "$attempt" == 5 ]] && exit 1
            sleep $((attempt * 5))
          done

      - name: Připravit SSH klíč
        if: steps.before.outputs.present != 'true'
        shell: bash
        run: |
          set -Eeuo pipefail
          umask 077
          for name in KEY_OVH_SSH_KEY KEY_OVH_SSH_PRIVATE_KEY KEY_SSH_PRIVATE_KEY KEY_VPS_SSH_KEY KEY_VPS_SSH_PRIVATE_KEY; do
            value="${!name:-}"
            if [[ -n "$value" ]]; then printf '%s\n' "$value" | tr -d '\r' > "$RUNNER_TEMP/ovh_key"; break; fi
          done
          test -s "$RUNNER_TEMP/ovh_key"
          chmod 600 "$RUNNER_TEMP/ovh_key"

      - name: Poslední přímé nasazení
        if: steps.before.outputs.present != 'true'
        shell: bash
        run: |
          set -euo pipefail
          tar -czf "$RUNNER_TEMP/alzbetinsky-rescue.tar.gz" \
            clanky/alzbetinsky-klaster-kadan-pacienti-lecebna-1966.html social/alzbetinsky-klaster-kadan-pacienti-20260803.png \
            index.html clanky/index.html rss.xml sitemap.xml news-sitemap.xml \
            site.js style.css footer.css mobile.css reklamy.js reklamy-sidebar.js reklamy-sidebar.css \
            reklamy-oprava-obrazku.js obsah-doplnky.js ad-spacing-guard.js analytics.js
          scp -i "$RUNNER_TEMP/ovh_key" -o BatchMode=yes -o StrictHostKeyChecking=accept-new -o ConnectTimeout=25 \
            "$RUNNER_TEMP/alzbetinsky-rescue.tar.gz" ubuntu@57.129.43.215:/tmp/alzbetinsky-rescue.tar.gz
          ssh -i "$RUNNER_TEMP/ovh_key" -o BatchMode=yes -o StrictHostKeyChecking=accept-new -o ConnectTimeout=25 ubuntu@57.129.43.215 'bash -s' <<'REMOTE'
          set -euo pipefail
          sudo tar -xzf /tmp/alzbetinsky-rescue.tar.gz -C /var/www/nasekadan
          sudo chmod -R a+rX /var/www/nasekadan
          if sudo docker inspect nasekadan-web >/dev/null 2>&1; then
            sudo docker cp /tmp/alzbetinsky-rescue.tar.gz nasekadan-web:/tmp/alzbetinsky-rescue.tar.gz
            sudo docker exec nasekadan-web tar -xzf /tmp/alzbetinsky-rescue.tar.gz -C /usr/share/nginx/html
            sudo docker exec nasekadan-web rm -f /tmp/alzbetinsky-rescue.tar.gz
          fi
          rm -f /tmp/alzbetinsky-rescue.tar.gz
          REMOTE

      - name: Definitivní veřejná kontrola
        id: verify
        shell: bash
        run: |
          set -euo pipefail
          for attempt in 1 2 3 4 5 6; do
            article="$(curl -fsSL --max-time 25 -H 'Cache-Control: no-cache' "https://nasekadan.cz/clanky/alzbetinsky-klaster-kadan-pacienti-lecebna-1966.html?definitive=${GITHUB_RUN_ID}-${attempt}" || true)"
            home="$(curl -fsSL --max-time 25 -H 'Cache-Control: no-cache' "https://nasekadan.cz/?definitive=${GITHUB_RUN_ID}-${attempt}" || true)"
            if grep -Fq 'Kam odešli poslední pacienti?' <<<"$article" &&
               grep -Fq 'data-poll-id="alzbetinsky-klaster-kadan-2026"' <<<"$article" &&
               grep -Fq 'data-latest-article-href="/clanky/alzbetinsky-klaster-kadan-pacienti-lecebna-1966.html"' <<<"$home"; then
              echo 'ok=true' >> "$GITHUB_OUTPUT"
              exit 0
            fi
            sleep $((attempt * 6))
          done
          exit 1

      - name: Zapsat definitivní výsledek
        if: always()
        uses: actions/github-script@v7
        env:
          OK: ${{ steps.verify.outputs.ok }}
          WAS_PRESENT: ${{ steps.before.outputs.present }}
        with:
          script: |
            const issue_number = 545;
            const ok = process.env.OK === 'true';
            const body = ok
              ? (process.env.WAS_PRESENT === 'true'
                  ? '✅ Poslední záchranná kontrola ve 4:50 potvrdila bezchybnou publikaci.'
                  : '✅ Poslední záchranná pojistka článek obnovila a veřejně ověřila.')
              : '🚨 KRITICKÉ: ani poslední záchranná pojistka nepotvrdila veřejnou publikaci. Issue zůstává otevřené pro okamžitý zásah.';
            await github.rest.issues.createComment({owner: context.repo.owner, repo: context.repo.repo, issue_number, body});
            if (ok) await github.rest.issues.update({owner: context.repo.owner, repo: context.repo.repo, issue_number, state: 'closed', state_reason: 'completed'});
